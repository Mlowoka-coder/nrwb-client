import React from 'react';
import { Card } from '@material-ui/core';

const List = () => {
    return (
        <React.Fragment>
            <Card>
                
            </Card>
        </React.Fragment>
    );
};

export default List;