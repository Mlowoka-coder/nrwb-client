import React from 'react';
import MainHeading from '../../Headings/MainHeading';

const UserSettings = () => {
    return (
        <React.Fragment>
            <MainHeading title='User Settings'/>
        </React.Fragment>
    );
};

export default UserSettings;